# Frontend Phase F0 — Stabilize

Completed:
- Fixed series/animation `view_count` and back-list links
- Global Django messages in `base.html`
- Font Awesome 6 CDN; Bootstrap JS deferred
- Download via `/downloads/<id>/` unlock (no raw CDN URL in HTML)
- Favorites/watchlist for movie, series, animation (+ remove on list pages)
- Removed duplicate Notifications nav item
